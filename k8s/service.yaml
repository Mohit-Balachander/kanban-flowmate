apiVersion: v1
kind: Service
metadata:
  name: kanban-flowmate-service
  labels:
    app: kanban-flowmate
spec:
  type: NodePort
  selector:
    app: kanban-flowmate
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
